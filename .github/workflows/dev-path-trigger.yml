name: Dev Path Trigger

on:
  push:
    branches:
      - dev
    paths:
      - 'src/**'

jobs:
  triggered:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print trigger message
        run: echo "Triggered by changes in src/ on dev branch"
